// test('Check if What the hell! does not contain heaven', () => {
//   const sentence = 'What the hell!';
//   expect(sentence).not.contain('heaven');
// });

test('Check if "Are you single?" does not contain taken', () => {
  const sentence = 'Are you single?';
  expect(sentence).not.contain('taken');
});
